#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void)
{
    char *p = "hello world ";
    char buf[128] = "good morning";

    len = strlen(p);
    //strcat (buf, p);
    for (i = 0; i < len; i++) 
    {
        
    }

    return 0;
}
